# Documentation site toolchain. See docs/contributing/building.md.
#
# Pinned so a docs build is reproducible and a surprise upstream release cannot
# break `mkdocs build --strict` in CI.
mkdocs==1.6.1
mkdocs-cinder==1.2.0
pymdown-extensions==10.21.3
pygments==2.21.0
